# Decision portfolio and types

This section describes recurring decision classes, decision questions, triggers, cadence, materiality and intended outcomes.

Canonical reusable classes are stored in `registers/decision-types.yaml`.
